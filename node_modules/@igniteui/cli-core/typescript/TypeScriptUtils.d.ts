import * as ts from "typescript";
export declare class TypeScriptUtils {
    static newLinePlaceHolder: string;
    /**
     * Returns an source file, adds new line placeholders as the TS parser won't add `SyntaxKind.NewLineTrivia` to the AST.
     * @param filePath Path of file to read
     */
    static getFileSource(filePath: string, useJSX?: boolean): ts.SourceFile;
    /**
     * Retrieves the source text, removes new line placeholders
     * @param source The source AST to print
     */
    static getSourceText(source: ts.SourceFile): string;
    static createSourceFile(filePath: string, text: string, scriptTarget: ts.ScriptTarget, setParentNodes?: boolean, useJSX?: boolean): ts.SourceFile;
    static createPrinter(): ts.Printer;
    /**
     * Saves the source to a target file
     * @param filePath File path
     * @param source Source AST to print
     */
    static saveFile(filePath: string, sourceFile: ts.SourceFile): any;
    static saveFile(filePath: string, content: string): any;
}
