import { MatchPercentagePipe } from './match-percentage.pipe';

describe('MatchPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new MatchPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
