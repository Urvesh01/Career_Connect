import { SalarySortPipe } from './salary-sort.pipe';

describe('SalarySortPipe', () => {
  it('create an instance', () => {
    const pipe = new SalarySortPipe();
    expect(pipe).toBeTruthy();
  });
});
