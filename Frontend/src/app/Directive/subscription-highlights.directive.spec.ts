import { SubscriptionHighlightsDirective } from './subscription-highlights.directive';

describe('SubscriptionHighlightsDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscriptionHighlightsDirective();
    expect(directive).toBeTruthy();
  });
});
